name: Legacy 0.4 Builder (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: windows-latest
    steps:
      - name: Deprecated pipeline notice
        shell: pwsh
        run: Write-Host "DXWeaver 0.4.x builder is frozen. Use windows-build.yml for the native 0.5+ product."
